"use strict";
const DEBUG_MODE = true;

/*
import { tasmotaAccess } from './api/tasmota';
tasmotaAccess.init("http://sonoff-0942");

if (false) {
    tasmotaAccess.getState().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-State: ', res);
        }
        console.log('Wifi: ', res.Wifi);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getStatus().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-Status: ', res);
        }
        console.log('Status FriendlyNmae: ', res.Status.FriendlyName);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getStatusPRM().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-StatusRPM: ', res);
        }
        console.log('Status Baudrate: ', res.StatusPRM.Baudrate);
        console.log('Status StartupUTC: ', res.StatusPRM.StartupUTC);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getStatusFWR().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-StatusFWR: ', res);
        }
        console.log('Status Version: ', res.StatusFWR.Version);
        console.log('Status BuildDateTime: ', res.StatusFWR.BuildDateTime);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getStatusFWR9().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-StatusFWR9: ', res);
        }
        console.log('Status Version: ', res.StatusFWR.Version);
        console.log('Status BuildDateTime: ', res.StatusFWR.BuildDateTime);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getHostname().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-Hostname: ', res);
        }
        console.log('Hostname: ', res.StatusFWR.Version);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getPower().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-Power: ', res);
        }
        console.log('Status Power: ', res.POWER);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getVoltRes().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-VoltRes: ', res);
        }
        console.log('Status Volt: ', res.VoltRes);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getPowerOnState().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-PowerOnState: ', res);
        }
        console.log('Status PowerOnState: ', res.PowerOnState);
    }).catch(console.error);
}

if (false) {
    tasmotaAccess.getPulseTime().then(res => {
        if (DEBUG_MODE) {
            console.log('Tasmota-PulseTime: ', res);
        }
        console.log('Status PulseTime: ', res.PulseTime1);
    }).catch(console.error);
}
*/